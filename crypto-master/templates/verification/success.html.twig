{% extends 'base.html.twig' %}

{% block title %}Erreur de vérification{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card">
        <div class="card-body text-center">
            <h2 class="text-danger">✗ Erreur de vérification</h2>
            <p class="mt-3">{{ message }}</p>
            <a href="{{ path('app_register') }}" class="btn btn-primary mt-3">Retour à l'inscription</a>
        </div>
    </div>
</div>
{% endblock %}